%h1 Eat
%p
  Kungsgatan 10, 100 00 Stockholm

- @products.each do |product|
  %div{id: "product-#{product.id}"}
    %h1= product.name
    = image_tag image_for(product)
    %div= add_to_cart(product, 1, 'Order')
